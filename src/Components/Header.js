import React from 'react';
import Logo from '../img/logo.png';

const Header = () => {
    return (
        <header>
            <img src={Logo} alt='Logo' />
        </header>
    );
};

export default Header;
